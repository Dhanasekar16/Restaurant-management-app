<!-- 1. Created centralised form component.

2. Created centralised modal component

3. Done CRUD operation using react redux

4. React bootstrap and sass preprocessor used

5. Data stored in local db file name (db.json)

6. To run local db use the command (npm run jsonServer)

7. To run app use command (npm start) -->